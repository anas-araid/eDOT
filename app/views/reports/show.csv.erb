<%- headers = ['Patient id', 'name', 'surname', 'address', 'gender', 'birthdate', 'phone','CHW', 'CHW id', 'Date', 'Health status'] -%>
<%= CSV.generate_line headers %>

<%= CSV.generate_line ([@report.patient_id, @report.patient.name, @report.patient.surname, @report.patient.address, @report.patient.gender, @report.patient.birthdate, @report.patient.phone, @report.community_health_worker.name,  @report.patient.community_health_worker_id,@report.date, @report.health_status]) -%>
